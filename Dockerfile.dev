# Development-only image for docker compose (hot reload via bind mount).
FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 2510

CMD ["npm", "run", "dev"]
